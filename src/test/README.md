# Tests

Esta carpeta replica la estructura de `src`. Cada componente con logica debe tener su test en la misma ruta relativa, usando el sufijo `.test.tsx`.

Ejemplo:

```text
src/modules/auth/pages/Login/Login.tsx
src/test/modules/auth/pages/Login/Login.test.tsx
```
